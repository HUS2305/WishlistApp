// Import gesture handler and reanimated at the very top for development builds
// This must be the first import to ensure proper initialization
import "react-native-gesture-handler";
import "react-native-reanimated";
import "expo-router/entry";
